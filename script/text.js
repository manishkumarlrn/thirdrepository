//Below config is to turn waitforaudio(audiolock) on/off by setting the value true/false

waitForAudio=false;

enableCourseAudio=true;

forceNavigation=true;

// tap click popup
enableTapPopupDesktop=false;
enableTapPopupMobile=false;
branding = true;

//Orientation Flag (set "true" to enable and "false" to disable)
phoneLandscapeFlg = false;
tabletPortraitFlg = false;
